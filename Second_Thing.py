from First_Thing import Area

"""
lengths = [4, 3, 12, 7]
widths = [6, 8, 2, 4]

if int(len(lengths)) == int(len(widths)):
    for x in range(len(lengths)):
        w = lengths[x]
        l = widths[x]
        Area = 1*w
        print("Area " + str(x) + " is equal to " + str(Area))
else:
     print("Your lists have different lengths")
"""
